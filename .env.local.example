# Neon PostgreSQL Connection String
# Get this from your Neon dashboard: https://console.neon.tech
DATABASE_URL=postgresql://user:password@ep-xyz.us-east-1.aws.neon.tech/neondb?sslmode=require

# Auth — generate a random 32+ char string
JWT_SECRET=cambia-esto-por-un-string-aleatorio-largo

# Admin setup key — call /api/admin/seed?key=ESTE_VALOR once to create users
ADMIN_SETUP_KEY=cambia-esto-tambien
